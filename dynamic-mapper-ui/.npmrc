# npm treats "engines" as advisory by default, so a wrong Node version produces confusing
# downstream failures (native sharp builds, Angular CLI aborts) instead of one clear message.
# engine-strict makes the declared range in package.json actually binding for install.
engine-strict=true
