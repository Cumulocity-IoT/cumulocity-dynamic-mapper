# This workflow will build a Java project with Maven
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Build Mapper

on:
  workflow_dispatch:
  push:
    # Sequence of patterns matched against refs/tags
    tags:
    - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: zulu
      #Building Starter backend
      - name: Build Performance Backend
        run: mvn -B package --file pom.xml
      - name: Rename file
        run: |
          mv ./dynamic-mapper-service/target/dynamic-mapper-service.zip ./dynamic-mapper-service/target/dynamic-mapper-service-starter.zip
      #Building Standard backend
      - name: Rename cumulocity file
        run: |
          mv ./dynamic-mapper-service/src/main/configuration/cumulocity-2c.json ./dynamic-mapper-service/src/main/configuration/cumulocity.json
      - name: Build Standard Backend
        run: mvn -B package --file pom.xml
        working-directory: ./
      - name: Rename file
        run: |
          mv ./dynamic-mapper-service/target/dynamic-mapper-service.zip ./dynamic-mapper-service/target/dynamic-mapper-service-standard.zip
      #Building performance backend
      - name: Rename cumulocity file
        run: |
          mv ./dynamic-mapper-service/src/main/configuration/cumulocity-8c.json ./dynamic-mapper-service/src/main/configuration/cumulocity.json
      - name: Build Performance Backend
        run: mvn -B package --file pom.xml
      - name: Rename file
        run: |
          mv ./dynamic-mapper-service/target/dynamic-mapper-service.zip ./dynamic-mapper-service/target/dynamic-mapper-service-performance.zip
      #Building max backend
      - name: Rename cumulocity file
        run: |
          mv ./dynamic-mapper-service/src/main/configuration/cumulocity-16c.json ./dynamic-mapper-service/src/main/configuration/cumulocity.json
      - name: Build Performance Backend
        run: mvn -B package --file pom.xml
      - name: Rename file
        run: |
          mv ./dynamic-mapper-service/target/dynamic-mapper-service.zip ./dynamic-mapper-service/target/dynamic-mapper-service-max.zip

      #Deployment
      - name: deploy
        uses: actions/setup-node@v2
        with:
          node-version: 18.x
      - run: npm i
        working-directory: ./dynamic-mapper-ui
      - run: cp ./LICENSE ./dynamic-mapper-ui/LICENSE
        working-directory: ./
      - run: npm install -g @angular/cli > /dev/null
        working-directory: ./dynamic-mapper-ui
      - run: npm run build --if-present
        working-directory: ./dynamic-mapper-ui
      - name: Zip Frontend Plugin
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        run: |
          cd dynamic-mapper-ui/dist/dynamic-mapper
          zip -r -q ../../dynamic-mapper-ui.zip *
      - name: Create Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: true
          prerelease: false
      - name: Upload Release Asset Frontend Blueprint
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        id: upload-release-asset-frontend
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./dynamic-mapper-ui/dynamic-mapper-ui.zip
          asset_name: dynamic-mapper-ui.zip
          asset_content_type: application/zip
      - name: Upload Release Asset Backend
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        id: upload-release-asset-starter
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./dynamic-mapper-service/target/dynamic-mapper-service-starter.zip
          asset_name: dynamic-mapper-service-starter.zip
          asset_content_type: application/zip
      - name: Upload Release Asset Backend
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        id: upload-release-asset-standard
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./dynamic-mapper-service/target/dynamic-mapper-service-standard.zip
          asset_name: dynamic-mapper-service-standard.zip
          asset_content_type: application/zip
      - name: Upload Release Asset Backend
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        id: upload-release-asset-performance
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./dynamic-mapper-service/target/dynamic-mapper-service-performance.zip
          asset_name: dynamic-mapper-service-performance.zip
          asset_content_type: application/zip
      - name: Upload Release Asset Backend
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        id: upload-release-asset-max
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./dynamic-mapper-service/target/dynamic-mapper-service-max.zip
          asset_name: dynamic-mapper-service-max.zip
          asset_content_type: application/zip

