/**
 * @name Shared code that is evaluated for every mapping and can be changed. It contains definitions used across different mappings
 * @templateType SHARED
 * @defaultTemplate true
 * @internal true
 * @readonly false
 * 
 * Shared code that is evaluated for every mapping and can be changed. It contains definitions used across different mappings
 */

// add your shared code relevant for all mappings here ...